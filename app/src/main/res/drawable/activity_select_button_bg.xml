<?xml version="1.0" encoding="UTF-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="wrap_content" android:layout_height="wrap_content" android:shape="rectangle">
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/linecolor" />
            <!-- 边框的颜色 ：不能和窗口背景色一样-->
            <stroke android:width="0dp" android:color="@color/linecolor" />
            <!-- 设置按钮的四个角为弧形 -->
            <!-- android:radius 弧形的半径 -->
            <corners android:radius="0dp" />

            <!-- padding：Button里面的文字与Button边界的间隔 -->
            <padding android:bottom="3dp" android:left="3dp" android:right="3dp" android:top="3dp" />
        </shape>

    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/white" />
            <!-- 边框的颜色 ：不能和窗口背景色一样-->
            <stroke android:width="0dp" android:color="@color/linecolor" />
            <!-- 设置按钮的四个角为弧形 -->
            <!-- android:radius 弧形的半径 -->
            <corners android:radius="0dp" />

            <!-- padding：Button里面的文字与Button边界的间隔 -->
            <padding android:bottom="3dp" android:left="3dp" android:right="3dp" android:top="3dp" />
        </shape>

    </item>
    <!-- 填充的颜色：这里设置背景透明 -->

</selector>